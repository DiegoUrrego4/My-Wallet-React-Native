import React from 'react';
import {Text, View} from 'react-native';
import {styles} from '../../theme/appTheme';

export const AccountScreen = () => {
  return (
    <View style={styles.globalMargin}>
      <Text style={styles.title}>AccountScrren</Text>
    </View>
  );
};
